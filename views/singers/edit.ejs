<h2 class="page-header">Edit Singer</h2>

<form action="/singers/<%= locals.singer.id%>?_method=PUT" method="POST">
  <%- include('_form_fields') %>
  <div class="form-row form-row-end btn-row">
    <a class="btn btn-danger" href="/singers/<%= locals.singer.id %>">Cancel</a>
    <button type="submit" class="btn btn-primary">Update</button>
  </div>
</form>